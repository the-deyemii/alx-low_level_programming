#include <stdio.h>
/**
 * main - Entry point
 * Return: Una papa
 */

int main(void)
{{
       	char ch = 'a';
	char ax = 'A';

	for (ch = 'a'; ch <= 'z'; ch++)
		putchar(ch);
	for (ax = 'A'; ax <= 'Z'; ax++)
		putchar(ax);

	putchar('\n')

		return (0);
}
