#ifndef MAIN_H
#define MAIN_H

#include <stdio.h>

void positive_or_negative(int i);
{
        if (n > 0)
        {
                printf("%i is positive\n", n);
        }
        else if (n == 0)
        {
                printf("%i is zero\n", n);
        }
        else if (n < 0)
        {
                printf("%i is negative\n", n);
        }

        return (0);
}
#endif /* MAIN_H */
